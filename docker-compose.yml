services:
  ip-server:
    build: .
    restart: unless-stopped
    environment:
      IP_SERVER_PORT: "8080"
    ports:
      # Public port can be changed when the hosting provider filters 8080.
      - "${IP_SERVER_PUBLIC_PORT:-8080}:8080"
